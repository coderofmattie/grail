;;----------------------------------------------------------------------
;; parser-fn.el
;;----------------------------------------------------------------------

;; Generic library routines used by parser.el.

(require 'cm-list)
(require 'cm-lisp)
(require 'cm-compat)

(provide 'parser-fn)
