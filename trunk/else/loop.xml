<?xml version="1.0" encoding="US-ASCII"?>
<else>
  <token xmlns:perl5="perl5" xmlns:java="java">
    <name>seq-index</name>
    <description>loop iteration through an array.</description>

    Sequential index is used where the ordering of the array allows
    the random access quality of the array to be exploited.

    <list>array name</list>

    <control>
      <prefix>$</prefix> <!-- Prefix is not yet implemented -->
      index variable name
    </control>

    <!-- <span> is the idea for chunks that would be loaded into named registers -->
    <perl5:body>
my $<r p="@control"/>; for ( $<r p="@control"/> = 0; $<r p="@control"/> &lt; @<r p="list"/>; $<r p="@control"/>++ ) {
}
<span name="array-ref"><r p="@list"/>[$<r p="@control"/>]</span>
    </perl5:body>

    <java:body>
      <!--
          Java section is under construction. Merely here to test the translater a bit.
      -->
    </java:body>
  </token>
</else>
