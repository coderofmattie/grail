<?xml version="1.0" encoding="US-ASCII"?>

<else>
  <token>
    <name>seq-index</name>
    loop iteration through a list

    <list>array name</list>
    <control>
      <prefix>$</prefix>
      index variable name
    </control>

    <!--
        Prefix is currently deferred.
    -->

    <!--
        <span> is the idea for chunks that would be loaded into named registers or something
        of the sort. currently ignored.
    -->

    <body>
my $<r p="@control"/>; for ( $<r p="@control"/> = 0; $<r p="@control"/> &lt; @<r p="list"/>; $<r p="@control"/>++ ) {
}
<span name="array-ref"><r p="@list"/>[$<r p="@control"/>]</span>
    </body>
  </token>
</else>
